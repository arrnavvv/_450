class Main {
    public int findDuplicate(int[] nums) {
        Arrays.sort(nums);
        int rep=0;
        for(int i=0;i<nums.length;i++){
            if(nums[i+1]==nums[i]){
                rep=nums[i];
                break;
            }
        }
        return rep;
    }
}
