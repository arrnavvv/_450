class Solution {
    int isPlaindrome(String S) {
        StringBuilder sb=new StringBuilder(S);
        sb.reverse();
        if(S.equals(sb.toString()))
        return 1;
        else
        return 0;
    }
};
